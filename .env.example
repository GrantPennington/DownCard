# Player token signing secret (use a long random string in production)
# Generate with: openssl rand -base64 32
PLAYER_TOKEN_SECRET=your-secret-key-min-32-chars-for-hmac-sha256-signing

# Game session secret (use a different random string)
GAME_SESSION_SECRET=your-game-session-secret-min-32-chars

# Default starting bankroll (in cents)
DEFAULT_BANKROLL_CENTS=100000

# Bet limits (in cents)
MIN_BET_CENTS=100
MAX_BET_CENTS=10000
